syntax = "proto3";
option java_package="ru.spbau.mit.kravchenkoyura.proto";

message Message {
    string name = 1;
    string text = 2;
}

service Messenger {
    rpc chat(stream Message) returns (stream Message);
}